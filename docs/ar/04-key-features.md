<div align="center" dir="rtl">

# 04 - الميزات الأساسية

> **تعمق في ميزات AstroShan الرئيسية وتنفيذها التقني**

---

## 🌌 1. محرك المجرة ثلاثي الأبعاد المتكيف (Adaptive 3D Galaxy Engine)

### نظرة عامة

تتميز الصفحة المقصودة بمجرة ثلاثية الأبعاد مذهلة مع ما يصل إلى 100,000 جسيم متحرك. هذا يخلق تأثيراً بصرياً فورياً بينما يبرز التطور التقني للمنصة.

### قصة المستخدم (User Story)

> بصفتي زائراً، أريد أن أرى تجربة بصرية مبهرة تلمح لجودة المنصة، بحيث أتحفز لاستكشاف المزيد.

### التنفيذ التقني

**المعمارية:**

```
┌─────────────────────────────────────────────────────────────────┐
│                 ADAPTIVE RENDERING PIPELINE                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌────────────────┐                                            │
│   │ useDeviceTier  │  Detects hardware via                      │
│   │                │  navigator.hardwareConcurrency             │
│   └───────┬────────┘                                            │
│           │                                                      │
│    ┌──────┴──────┬──────────────┐                               │
│    │             │              │                                │
│   HIGH         MEDIUM         LOW                                │
│    │             │              │                                │
│    ▼             ▼              ▼                                │
│  ┌────────┐  ┌────────┐   ┌───────────────┐                     │
│  │ WebGL  │  │ WebGL  │   │ Canvas 2D     │                     │
│  │ 100k   │  │ 25k    │   │ StaticBackground│                   │
│  │particles│ │particles│  │ requestIdleCallback│                │
│  └────────┘  └────────┘   └───────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**التقنيات الرئيسية:**
- `Three.js` عبر `@react-three/fiber`: للعرض ثلاثي الأبعاد
- `Web Workers`: لحساب الجسيمات خارج المسار الرئيسي (Off-thread)
- `useDeviceTier` hook: لاكتشاف العتاد
- `Canvas 2D API`: كبديل للعرض (Fallback renderer)

**تدفق البيانات:**

1. تحميل الصفحة → تشغيل `useDeviceTier`
2. الـ Hook يفحص `navigator.hardwareConcurrency`
3. بناءً على المستوى (Tier)، يتم عرض إما `InteractiveGalaxy` أو `StaticBackground`
4. إذا كان WebGL، يقوم `useGalaxyWorker` بإنشاء Web Worker
5. يقوم الـ Worker بحساب المواقع/الألوان لكل الجسيمات
6. يستقبل المسار الرئيسي `Float32Array` (نقل zero-copy)
7. يتم تحديث `BufferAttribute` في Three.js بالبيانات الجديدة

**التحديات:**
- التنفيذ الأولي تسبب في 14 ثانية TBT على الموبايل
- اكتشاف WebGL قد يكون غير موثوق
- استخدام الذاكرة مع 100k جسيم

**الحل:**
- Web Worker لكل الحسابات المرتبطة بـ GPU
- اكتشاف WebGL قوي مع خيار بديل (Fallback)
- عدد جسيمات متكيف بناءً على عرض الشاشة
- استخدام `requestIdleCallback` للتحميل المؤجل

### مقتطفات الكود

```typescript
// Device tier detection - simplified
export function useDeviceTier(): DeviceTier {
  const [tier, setTier] = useState<DeviceTier>("low");

  useEffect(() => {
    const timer = setTimeout(() => {
      const cores = navigator.hardwareConcurrency || 4;
      if (cores >= 6) setTier("high");
      else if (cores >= 2) setTier("medium");
    }, 0);
    return () => clearTimeout(timer);
  }, []);

  return tier;
}
```

### اعتبارات الأداء

| المقياس | قبل التحسين | بعد التحسين |
|--------|--------------------|--------------------|
| TBT (موبايل) | 14,000ms | 2,500ms |
| توليد الجسيمات | Main thread | Web Worker |
| استخدام الذاكرة | ثابت 100k | متكيف 12k-100k |

**رسم توضيحي:**

![Web Worker Galaxy Engine](../../diagrams/webworker_galaxy_fixed_1768603947661.webp)

<br/>

<div align="center">
<img width="600" src="https://raw.githubusercontent.com/andreasbm/readme/master/assets/lines/rainbow.png"/>
</div>

<br/>

## 🏠 2. البيت الهولوغرافي CSS 3D

### نظرة عامة

الـ BlueprintHouse هو مؤشر تقدم بصري مبني بالكامل بتحويلات CSS 3D. مع إكمال الطلاب للدروس، يبني المنزل نفسه – من الأساس إلى السقف فالتفاصيل الزخرفية.

### قصة المستخدم

> بصفتي طالباً، أريد رؤية تقدمي مصوراً بطريقة جذابة، لكي أشعر بالحافز للاستمرار في التعلم.

### التنفيذ التقني

**المعمارية:**

يستخدم المنزل `transform-style: preserve-3d` في CSS لخلق منظور ثلاثي أبعاد حقيقي دون WebGL.

```
BlueprintHouse (512 lines)
├── Container (perspective: 1000px)
│   ├── HoloBase (Foundation)
│   │   └── Visible at 0-15% progress
│   │
│   ├── HoloFace × 4 (Walls)
│   │   └── Visible at 15-55% progress
│   │
│   ├── HoloRoofFace × 2 (Roof panels)
│   │   └── Visible at 55-80% progress
│   │
│   └── Details + OrbitingRing
│       └── Visible at 80-100% progress
```

**التقنيات الرئيسية:**
- `CSS transform-style: preserve-3d`: الحفاظ على الفضاء ثلاثي الأبعاد
- `Framer Motion`: انتقالات الشفافية/التحجيم
- `MotionValue`: الظهور المدفوع بالتقدم
- `useTransform`: استيفاء سلس (Smooth interpolation)

**لماذا CSS 3D بدلاً من Three.js؟**

| الاعتبار | CSS 3D | Three.js |
|---------------|--------|----------|
| حمل الـ Main thread | ضئيل | ثقيل |
| استخدام GPU | Browser compositing | WebGL context |
| توافق الأجهزة | عالمي | يتطلب WebGL |
| تعقيد التنفيذ | متوسط | أعلى |
| الجودة البصرية | جيدة كفاية | متفوقة |

**الظهور المدفوع بالتقدم:**

```typescript
// Each component uses progress-driven opacity
const opacity = useTransform(
  progress,
  [triggerRange[0], triggerRange[1]],  // e.g., [0.15, 0.55]
  [0, 1]                                // fade in
);
```

### الحالات (States)

**الحالة العادية:** توهج سماوي/أزرق (#1eb8e4)
**حالة الانتظار:** ثيم أصفر/تحذير (الطالب يحتاج لمتطلبات مسبقة)

### اعتبارات الأداء

- استخدام تعزيز طبقة التركيب (`will-change`)
- توقف الأنيميشن عند فتح نافذة الدرس
- OrbitingRing تستخدم CSS animations (ليس JS)

---

## ✅ 3. محرك التحقق من الكود المباشر

### نظرة عامة

يقوم TaskChecklist بتحليل كود الطالب في الوقت الفعلي، متحققاً من هيكل الـ DOM والمحتوى مقابل متطلبات الدرس.

### قصة المستخدم

> بصفتي طالباً يكتب الكود، أريد تغذية راجعة فورية عما إذا كان كودي يلبي المتطلبات، لأصحح أخطائي بينما السياق لا يزال حاضراً.

### التنفيذ التقني

**المعمارية:**

```
┌────────────────────────────────────────────────────────────────┐
│                    VALIDATION PIPELINE                          │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Student Code         DOMParser            Validators          │
│   (string)       →     (parse)      →      (check)      →      │
│                                                                 │
│   "<h1>Hello</h1>"  →  Document  →  [                          │
│                                       { type: "exists",         │
│                                         selector: "h1",         │
│                                         passed: true },         │
│                                       { type: "content",        │
│                                         regex: /Hello/,        │
│                                         passed: true }          │
│                                     ]  →   UI Update            │
206 └────────────────────────────────────────────────────────────────┘
```

**التقنيات الرئيسية:**
- `DOMParser API`: تحليل HTML آمن (لا خطر XSS)
- `Regex validators`: فحص المحتوى
- `React state`: تحديثات UI حية

**أنواع التحقق:**

| النوع | الوصف | مثال |
|------|-------------|---------|
| `elementExists` | التحقق من وجود العنصر | `<header>` موجود؟ |
| `elementContent` | التحقق من النص | `<title>` يحتوي "Portfolio"؟ |
| `attributeExists` | التحقق من السمة (Attribute) | `<img>` لديه `alt`؟ |
| `attributeValue` | التحقق من قيمة السمة | `lang="ar"`؟ |

**تلميحات تدريجية:**

عند فشل التحقق، تظهر التلميحات تدريجياً:
1. المحاولة الأولى: تلميح عام
2. بعد 30 ثانية: تلميح أكثر تحديداً
3. بعد 60 ثانية: حل شبه كامل

### تكامل حارس البوابة (Gatekeeper Integration)

```typescript
// Tasks lock if student isn't authorized
<TaskChecklist
  requirements={taskRequirements}
  code={studentCode}
  onValidationChange={setIsValid}
  isAuthorized={isAuthorized}  // from useGatekeeper
/>
```

---

## 📝 4. نظام المهام التراكمي

### نظرة عامة

بخلاف التمارين المعزولة، تبني مهام AstroShan فوق بعضها البعض. ينتقل الكود من مهمة إلى التالية، متوجاً بموقع معرض أعمال كامل.

### قصة المستخدم

> بصفتي طالباً، أريد أن يستمر كودي عبر الدروس، لأبني مشروعاً حقيقياً بدلاً من مقتطفات معزولة.

### التنفيذ التقني

**خوارزمية اكتشاف المهام السابقة (Backward Task Discovery):**

```
┌─────────────────────────────────────────────────────────────────┐
│                 CODE LOADING DECISION TREE                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   CodeEditorView.loadCode()                                      │
│        │                                                         │
│        ▼                                                         │
│   ┌────────────────────────────┐                                │
│   │ Check saved progress for   │                                │
│   │ current task ID            │                                │
│   └────────────┬───────────────┘                                │
│                │                                                 │
│       ┌────────┴────────┐                                       │
│       │                 │                                        │
│   [Exists]          [Not Found]                                  │
│       │                 │                                        │
│       ▼                 ▼                                        │
│   Return saved    ┌────────────────────────────┐                │
│   code            │ Find previous mission      │                │
│                   │ in same module             │                │
│                   └────────────┬───────────────┘                │
│                                │                                 │
│                       ┌────────┴────────┐                       │
│                       │                 │                        │
│                   [Found]           [Not Found]                  │
│                       │                 │                        │
│                       ▼                 ▼                        │
│                   Return            Return                       │
│                   previous          starterCode                  │
│                   code                                           │
│                   code                                           │
│                                           │
└─────────────────────────────────────────────────────────────────┘
```

**الحفظ التلقائي (Auto-Save):**
- تأخير (Debounced) بمدة 500ms
- localStorage أساسي
- مزامنة MongoDB إذا كان مسجلاً

**تسلسل المهام:**
- L24: تخطيط الأساس (Foundation layout)
- L36: المعرض والخدمات
- L50: إعادة هيكلة دلالية (Semantic refactor)
- L57: إطلاق المعرض (Capstone)

**رسم توضيحي:**

![Backward Task Discovery](../../diagrams/backward_task_discovery_bilingual_1768602050087.webp)

## 🔐 5. نظام المصادقة ثنائي الرموز (Dual-Token Auth)

### نظرة عامة

أمان JWT بمستوى المؤسسات مع تجربة مستخدم سلسة. يستخدم رموز وصول قصيرة العمر (ذاكرة فقط) ورموز تحديث طويلة العمر (HttpOnly cookies).

### قصة المستخدم

> كمستخدم، أريد أن تستمر جلستي دون انقطاع، مع معرفة أن حسابي آمن.

### التنفيذ التقني

**معمارية التوكن:**

```
┌─────────────────────────────────────────────────────────────────┐
│                    DUAL TOKEN SYSTEM                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ACCESS TOKEN                    REFRESH TOKEN                  │
│   ├── Lifetime: 15 minutes        ├── Lifetime: 7 days          │
│   ├── Storage: In-memory only     ├── Storage: HttpOnly cookie  │
│   ├── Contains: userId, email     ├── Contains: userId          │
│   └── Used for: API calls         └── Used for: Token refresh   │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   خصائص الأمان:                                                  │
│   ✅ حماية XSS: Access token ليس في localStorage أبداً          │
│   ✅ حماية CSRF: كوكيز التحديث SameSite=Strict                  │
│   ✅ تخفيف السرقة: نافذة 15 دقيقة حتى لو سُرق التوكن            │
│   ✅ تدوير صامت: المستخدم لا يرى تحديث التوكن أبداً             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**تدفق التحديث الصامت (Silent Refresh Flow):**

```
API Call → 401 Unauthorized
     │
     ▼
AuthStore.refreshAccessToken()
     │
     ├── POST /api/auth/token/refresh
     │   (cookie sent automatically)
     │
     ▼
┌────────────────────────────┐
│ Server verifies refresh    │
│ token, issues new access   │
│ token in response body     │
└────────────┬───────────────┘
             │
             ▼
Store in memory → Retry original request
```

**منع التكرار (Deduplication):**
```typescript
// Prevents multiple concurrent refresh attempts
if (this.refreshPromise) return this.refreshPromise;

this.refreshPromise = (async () => {
  // ... refresh logic
})();
```

**رسم توضيحي:**

![Anonymous-First Auth](../../diagrams/anonymous_first_auth_1768725256945.webp)

## 🧩 6. مكتبة الأدوات التفاعلية (Interactive Widget Library)

### نظرة عامة

35+ مكون React مخصص يصور المفاهيم المجردة من خلال المحاكاة الفيزيائية، ألغاز السحب والإفلات، والمخططات التفاعلية.

### فئات الأدوات

**محاكاة فيزيائية:**
- `TextMagnetSim`: تصور المجال المغناطيسي لاتجاه النص RTL/LTR
- `TLSHandshakeSim`: أنيميشن مصافحة الأمان

**شروح بصرية:**
- `CommentGoggles`: رؤية بالأشعة السينية لتعليقات HTML
- `SkeletonBuilder`: لغز هيكل HTML5
- `SemanticElevator`: ناطحة سحاب لتسلسل العناوين

**محاكاة المجال:**
- `DNSSimulator`: أنيميشن البحث في DNS
- `ClientServerSim`: دورة حياة الطلب/الاستجابة
- `FileExplorerSim`: تصفح مسارات الملفات

### مجموعة واجهة الأدوات (Widget UI Kit)

تنسيق متسق وإمكانية وصول عبر جميع الأدوات:

```
WidgetContainer
├── WidgetHeader (title, icon, reset button)
├── [Interactive Content]
└── WidgetSuccessState (completion animation)
```

**إمكانية الوصول (Accessibility):**
- تنقل كامل بالكيبورد
- `WidgetToggleGroup` للاختيار الشبيه بالتبويبات
- تسميات متوافقة مع قوارئ الشاشة

---

## 🎭 7. قسم Hero المدرك للسياق (Context-Aware Hero)

### نظرة عامة

يتكيف الـ Hero في الصفحة المقصودة ديناميكياً بناءً على حالة المستخدم – عارضاً محتوى مختلفاً، وتوجيهات (CTAs)، وأنيميشن لكل مرحلة من الرحلة.

### المتغيرات (Variants)

| الحالة | منطق الاكتشاف | المحتوى |
|-------|-----------------|---------|
| **زائر** | لا توجد جلسة | "ابدأ رحلتك" |
| **زائر + تقدم** | مجهول + بيانات localStorage | "تقدمك محفوظ محلياً" |
| **مستكشف جديد** | مسجل + 0 دروس | "أهلاً، كابتن [الاسم]!" |
| **قيد التقدم** | مسجل + بعض الإنجاز | "متابعة الرحلة، X% تم" |
| **مسافر الجسر** | أنهى الإنترنت، لم يبدأ HTML | "جاهز للـ HTML؟" |
| **سيد الكورس** | الوحدة مكتملة | "الكوكب تم غزوه!" |

### تنفيذ آمن للـ Hydration

نص الـ Hero يتم عرضه من جانب السيرفر لـ LCP، ثم يقوم العميل بالتحديث بناءً على حالة المصادقة:

```typescript
// Server: Render default hero text
// Client: useEffect checks auth state, updates variant
useEffect(() => {
  const determineVariant = async () => {
    const session = await authStore.getSession();
    const progress = await progressStore.getOverallProgress();
    
    if (!session.isAuthenticated) {
      if (progress.totalCompleted > 0) return "guestWithProgress";
      return "guest";
    }
    // ... more logic
  };
}, []);
```

---

## 🔗 وثائق ذات صلة

- [← معمارية الحل](03-solution-architecture.md)
- [القرارات التقنية →](05-technical-decisions.md)
- [التحديات والحلول →](06-challenges-solutions.md)

---

*التالي: [05 - القرارات التقنية](05-technical-decisions.md)*

</div>

<div align="center">

[![Prev: Solution Architecture](https://img.shields.io/badge/←_Prev:_Solution_Architecture-1eb8e4?style=for-the-badge)](03-solution-architecture.md) [![Next: Technical Decisions](https://img.shields.io/badge/Next:_Technical_Decisions_→-7565e3?style=for-the-badge)](05-technical-decisions.md)

</div>
